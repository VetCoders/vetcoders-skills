interface:
  display_name: "VetCoders Prune"
  short_description: "Prune repos to runtime or publish cone"
  default_prompt: "Use $vetcoders-prune to classify runtime/build/publish surface, route non-trivial prune work through external VetCoders agents, and plan a proof-driven cleanup."
